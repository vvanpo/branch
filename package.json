{
  "name": "titian",
  "version": "0.0.0",
  "repository": "github:vvanpo/titian",
  "license": "GPL-3.0",
  "scripts": {
    "clean": "rm -rf www dist",
    "dev": "stencil build --dev --watch --serve",
    "lint": "tslint -p . --fix"
  },
  "dependencies": {
    "redux": "^4.0.1",
    "yargs": "^12.0.5"
  },
  "devDependencies": {
    "@stencil/core": "^0.16.0",
    "@stencil/sass": "^0.1.1",
    "tslint": "^5.11.0",
    "typescript": "^3.2.2"
  }
}
